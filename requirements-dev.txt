# Use Python 3.11+ when creating the venv for this test/dev environment.
-r requirements.txt

pytest>=8,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5,<8

# Property-based testing (governance plumbing — see Phase 1 plan)
hypothesis>=6,<7

# Static analysis — run `ruff check .` locally before committing.
# Config lives in pyproject.toml.
ruff>=0.6,<1
